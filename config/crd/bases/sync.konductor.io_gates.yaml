---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: gates.sync.konductor.io
spec:
  group: sync.konductor.io
  names:
    kind: Gate
    listKind: GateList
    plural: gates
    singular: gate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.conditions[0].name
      name: Conditions
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Gate is the Schema for the gates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GateSpec defines the desired state of Gate
            properties:
              conditions:
                description: Conditions that must be met for the gate to open
                items:
                  description: GateCondition defines a condition that must be met
                  properties:
                    name:
                      description: Name of the resource to check
                      minLength: 1
                      type: string
                    namespace:
                      description: Namespace of the resource (optional, defaults to
                        gate's namespace)
                      type: string
                    state:
                      description: |-
                        State required for the condition to be met
                        For Job: Complete, Failed, or Active
                        For Barrier: Open or Closed
                        For Lease: Acquired or Available
                        For Gate: Open or Closed
                        For Mutex: Locked or Unlocked
                        For Once: Done or Pending
                        For WaitGroup: Zero or NonZero
                      enum:
                      - Complete
                      - Failed
                      - Active
                      - Open
                      - Closed
                      - Acquired
                      - Available
                      - Locked
                      - Unlocked
                      - Done
                      - Pending
                      - Zero
                      - NonZero
                      type: string
                    type:
                      description: Type of condition (Job, Semaphore, Barrier, Lease)
                      enum:
                      - Job
                      - Semaphore
                      - Barrier
                      - Lease
                      - Gate
                      - Mutex
                      - Once
                      - WaitGroup
                      type: string
                    value:
                      description: Value for numeric conditions (e.g., semaphore permits)
                      format: int32
                      type: integer
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              timeout:
                description: Timeout for waiting for conditions
                type: string
            required:
            - conditions
            type: object
            x-kubernetes-validations:
            - message: timeout must be a valid duration (e.g., 30s, 5m, 1h)
              rule: '!has(self.timeout) || self.timeout.matches(''^([0-9]+(\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$'')'
          status:
            description: GateStatus defines the observed state of Gate
            properties:
              conditionStatuses:
                description: ConditionStatuses tracks the status of each condition
                items:
                  description: GateConditionStatus tracks the status of a gate condition
                  properties:
                    message:
                      description: Message provides details about the condition status
                      type: string
                    met:
                      description: Met indicates if the condition is satisfied
                      type: boolean
                    name:
                      description: Name of the resource
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - met
                  - name
                  - type
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              openedAt:
                description: OpenedAt is when the gate opened
                format: date-time
                type: string
              phase:
                description: Phase represents the current state of the gate
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
